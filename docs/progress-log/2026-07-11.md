# 2026-07-11

- Bàn lộ trình tổng thể (contract → backend → frontend → hạ tầng)
- Giải thích cấu trúc thư mục monorepo và vai trò từng phần
- Giải thích `.claude/` (skills, sub-agent, hook) và tạo ví dụ mẫu
- Tạo khung thư mục đầy đủ: `contracts/`, `indexer/`, `backend/`, `frontend/`, `shared/`, `assets/`, `infra/`, `docs/`
- Tạo `CLAUDE.md`, `README.md`, `.gitignore`, `.claude/settings.json`
- Tạo agent mẫu `contract-auditor`, skill mẫu `deploy-contract`
- Bàn về chi phí phát triển vs vận hành on-chain cho model Idle/Farming
- Thiết lập `PROGRESS.md` + `TASKS.md` để theo dõi tiến độ qua nhiều phiên
- Tách nhật ký theo ngày ra `docs/progress-log/` để tránh PROGRESS.md phình to tốn token đọc lại
- Di chuyển toàn bộ project vào thư mục con `nft-farm-game/`
- Cài GitHub CLI (`gh`), đăng nhập, `git init` + commit đầu tiên
- Tạo repo public và push lên https://github.com/Pledat02/nft-farm-game
- Lập kế hoạch tự xây dựng dự án (plan mode), thống nhất nguyên tắc an toàn: không xóa file, không rewrite git history, chỉ testnet, không tự deploy
- Milestone 1: viết `docs/game-design.md` + `docs/tokenomics.md` (quyết định mặc định: Idle/Farming, NFT=nhân vật, chưa có token ERC-20, chain Base Sepolia), tick xong Giai đoạn 0 trong TASKS.md
- Milestone 2: cài Foundry (foundryup, verify attestation), setup `contracts/foundry.toml`, cài `forge-std` + `openzeppelin-contracts` qua `forge install`, viết `contracts/src/GameNFT.sol` (mint/claim/pendingRewards), sửa remapping OpenZeppelin, `forge build` thành công
- Milestone 3: viết `contracts/test/GameNFT.t.sol` (7 test case: mint, claim theo thời gian, claim=0 nếu chưa qua thời gian, revert khi không phải chủ sở hữu, pendingRewards khớp claim, claim 2 lần cộng dồn đúng, tokenId tăng tuần tự) — `forge test` 7/7 pass
- Milestone 4: rà bảo mật thủ công theo checklist agent `contract-auditor` (agent tuỳ chỉnh không được harness đăng ký làm subagent_type, nên tự review trực tiếp). Phát hiện 1 vấn đề CEI-ordering trong `mint()` (set state trước _safeMint để tránh reentrancy window), đã sửa; 2 design note không phải lỗ hổng (mint không giới hạn, Ownable chưa dùng tới) — ghi nhận để sau. Test lại 7/7 pass
- Push 4 commit milestone 1-4 lên GitHub
- Deploy testnet: tạo `contracts/script/DeployGameNFT.s.sol` + `.env.example`, hướng dẫn người dùng tự tạo ví (`cast wallet new`) và faucet ở terminal riêng (không share private key qua chat). Faucet đầu tiên (Alchemy) yêu cầu số dư mainnet nên đổi hướng dẫn faucet khác. Deploy thành công GameNFT lên Base Sepolia tại `0x8F2AfEd590CF2C83a08672AFdf341D5c388ebAF6` (chain 84532) — xem [docs/deployments.md](../deployments.md). Kiểm tra broadcast/cache artifact không chứa private key trước khi commit
- Bắt đầu Giai đoạn 2 (Backend): viết `docs/backend-design.md` (Express+TS, Prisma+SQLite, viem, JWT, luồng auth SIWE-đơn-giản-hoá). Scaffold `backend/` (package.json, tsconfig, Prisma schema Player/FarmerNFT/ClaimEvent), export ABI GameNFT vào `shared/abi/GameNFT.json`. Viết API `/auth/nonce`, `/auth/verify`, `/inventory/:address`, `/leaderboard`. `npm install` + `prisma migrate dev` chạy OK
- Test thủ công qua curl: phát hiện bug nghiêm trọng — gửi signature sai định dạng làm **crash toàn bộ server** (unhandled exception trong route async). Sửa: validate format signature bằng regex trước, bọc try/catch quanh `verifyMessage`, thêm error-handling middleware toàn cục trong Express. Test lại xác nhận server không còn crash
- Viết indexer (`indexer/`): package riêng (chưa có npm workspaces), Prisma schema bản sao trỏ cùng file SQLite với backend (`../../backend/prisma/dev.db`), dùng viem `watchContractEvent` cho `Minted`/`Claimed`, hàm `syncToken()` đọc lại state on-chain (ownerOf/level/lastClaimTime/resourcePoints) thay vì tự suy ra từ event args — tránh lệch dữ liệu
- Test end-to-end thật trên Base Sepolia: chạy indexer nền, `cast send mint()` → indexer log "synced tokenId=0" trong vài giây, `/inventory` trả đúng owner/level. `cast send claim(0)` → resourcePoints cập nhật đúng 0 → 34, khớp thời gian trôi qua thực tế. Dừng toàn bộ process test sau khi xác nhận. Giai đoạn 2 (Backend) hoàn tất
- Push 2 commit Giai đoạn 2 lên GitHub
- Bắt đầu Giai đoạn 3 (Frontend): viết `docs/frontend-design.md` (Next.js App Router, wagmi+viem không dùng RainbowKit để giảm dependency, đọc "NFT của tôi" qua backend /inventory vì GameNFT không có extension Enumerable nên không thể liệt kê on-chain trực tiếp)
- Scaffold Next.js: `create-next-app` chạy trong thư mục scratch riêng (vì `frontend/` đã có README.md + thư mục src/ rỗng từ đầu, create-next-app không chịu chạy non-interactive trên thư mục non-empty), copy thủ công các file cần thiết vào `frontend/`, giữ nguyên README.md và các thư mục rỗng cũ (không xóa gì)
- Viết `lib/web3.ts` (wagmi config), `lib/contract.ts` (copy riêng ABI GameNFT — tương tự cách indexer đã làm với Prisma schema), component `ConnectButton`/`MintButton`/`FarmerCard`, hook `useFarmerNFTs`/`useLeaderboard`, trang chính `app/page.tsx` + `app/leaderboard/page.tsx`
- `tsc --noEmit` + `eslint` sạch sau khi sửa 1 lỗi type (`pending` possibly null)
- Test qua Browser tool thật: tạo `.claude/launch.json` (phải đặt ở root `Claude Project/.claude/`, không phải trong repo — do working directory chính của session là thư mục cha). Trang chính render đúng khi chưa kết nối ví. Trang leaderboard lúc đầu fetch lỗi `net::ERR_FAILED` — phát hiện bug **thiếu CORS** ở backend (Express chưa có middleware `cors()`), cài `cors` + `@types/cors`, thêm `app.use(cors())`, test lại thành công: leaderboard hiển thị đúng Farmer #0 thật (34 points) từ Base Sepolia. Không test được thao tác click ví thật vì browser sandbox không có extension MetaMask. Giai đoạn 3 (Frontend) hoàn tất, trừ Marketplace (hoãn theo thiết kế)
- Chạy local đầy đủ (backend + indexer + frontend) để người dùng tự test bằng trình duyệt thật. Gặp lỗi Turbopack "Cannot find module 'wagmi'" do `.next/` cache cũ từ lần chạy trước đó — xóa `.next/` (build artifact, an toàn) rồi chạy lại là hết
- Người dùng báo lỗi `Runtime TypeError: Cannot redefine property: ethereum` từ `evmAsk.js` (script của ví Phantom) — xác định đây là xung đột giữa nhiều extension ví (Phantom + MetaMask) cùng cố ghi đè `window.ethereum`, không phải bug code. Theo yêu cầu người dùng, sửa `ConnectButton.tsx` để liệt kê tất cả connector phát hiện được qua chuẩn EIP-6963 (wagmi v2 bật `multiInjectedProviderDiscovery` mặc định) thành dropdown, cho người dùng tự chọn đúng ví muốn dùng thay vì code tự lấy `connectors[0]`. Test qua browser: dropdown hiện đúng danh sách connector, không lỗi
- Người dùng phản hồi giao diện xấu, yêu cầu hoàn thiện. Dùng skill `ui-ux-pro-max` (Quick Reference, Python không có sẵn nên không chạy được search CLI, tự thiết kế dựa trên guideline). Xây design system: màu xanh lá (primary, nông trại) + vàng hổ phách (accent, phần thưởng) trên nền ấm, hỗ trợ dark mode qua CSS variable + Tailwind v4 `@theme inline`; font Baloo 2 (heading) + Inter (body); icon `lucide-react` thay vì emoji. Cài `lucide-react`. Viết `components/Header.tsx` dùng chung, redesign toàn bộ `ConnectButton`/`MintButton`/`FarmerCard`/trang chính/leaderboard: card bo góc, skeleton loading, empty state có icon, rank badge huy chương cho leaderboard
- Sửa 1 lỗi TypeScript (`BigInt literals are not available when targeting lower than ES2020` — do tsconfig target ES2017 mặc định của create-next-app) bằng cách dùng `BigInt(0)` thay vì literal `0n`
- Test qua browser: `.next/` cache cũ khiến CSS/font mới không load (phải xóa `.next/` + khởi động lại — cùng loại lỗi cache đã gặp trước đó). Sau khi restart sạch, xác nhận qua `getComputedStyle` (không chỉ nhìn ảnh chụp — công cụ screenshot render sai kích thước dù DOM đúng) rằng theme mới đã áp dụng đúng (nền `#faf8f3`, nút xanh `#3f7d3a`, font Baloo 2/Inter). Test responsive mobile (375px): icon-only nav, ẩn cột phụ trong bảng, không tràn ngang

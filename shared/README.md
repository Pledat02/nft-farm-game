# Shared

Type và ABI dùng chung giữa backend và frontend, tránh lệch phiên bản khi contract thay đổi.

- `abi/` — ABI export tự động từ contracts sau mỗi lần compile
- `types/` — TypeScript type dùng chung

# Frontend

Web app / game client.

- `src/components/` — UI component
- `src/pages/` — Trang chính (game, marketplace, inventory...)
- `src/hooks/` — Custom hook, ví dụ useWallet, useContract
- `src/lib/` — Cấu hình web3 (wagmi/viem), RPC endpoint

# Copy file này thành .env (đã có trong .gitignore, không commit) và điền giá trị thật.
# KHÔNG BAO GIỜ dán private key ví thật có tiền thật vào đây — chỉ dùng ví testnet riêng, không có giá trị thật.

BASE_SEPOLIA_RPC_URL=https://sepolia.base.org
PRIVATE_KEY=
